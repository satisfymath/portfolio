# Riesgos y seguridad

- **Riesgo de performance** en grafos grandes → controles de render (opacidad contextual, minimap opcional).
- **Riesgo de estado inconsistente** → helpers de normalización/serialización y contratos de datos.
- **Privacidad**: export local; sin envío de datos por defecto.
